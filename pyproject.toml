[project]
name = "pizza-delivery"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
]

[tool.pytest.ini_options]
testpaths = ["app/tests"]
pythonpath = ["."]
